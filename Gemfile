source "https://rubygems.org"

# Use GitHub Pages gem to match the GitHub Pages build environment
gem "github-pages", group: :jekyll_plugins

# Needed for local server (Ruby 3+ requires this)
gem "webrick", "~> 1.8"
